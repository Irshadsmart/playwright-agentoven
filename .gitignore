
# Playwright
node_modules/
/test-results/
/playwright-report/
/blob-report/
/playwright/.cache/
/playwright/.auth/

# Generated HTML reports (large, not needed in source control)
/reports/

# Keep backup/ Excel files tracked in git (that's the whole point)
