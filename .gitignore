
# Playwright
node_modules/
/test-results/
/playwright-report/
/blob-report/
/playwright/.cache/
/playwright/.auth/

# Generated HTML reports (large, not needed in source control)
/reports/

# AgentOven Docker backups — keep locally (can be large), don't push to GitHub
/backup/

# data/data.xlsx is pushed (Excel backup from export-to-excel.js)
